spring:
  application:
    name: admin-service
  cloud:
    nacos:
        server-addr: st.zeus.cn:8848
        config:
          file-extension: yaml
  profiles:
    active: dev
# 拉取的配置文件的dataID = admin-service-dev.yaml